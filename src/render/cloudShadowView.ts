// Drifting cloud shadows (above the bodies, below the UI). Phase 2B M5 fills this with a few
// large soft dark patches sliding slowly across the field (optional asset "cloud", else
// procedural) — the spec's standout beauty-per-effort item. M0 stub: empty.

import { Container } from "pixi.js";

export class CloudShadowView {
  readonly container = new Container();
}
